SELECT ENAME
FROM EMP
WHERE ENAME LIKE 'S%';

SELECT ENAME
FROM EMP
WHERE ENAME LIKE '_M%';

SELECT *
FROM EMP
WHERE ENAME LIKE '%''%';

INSERT INTO EMP (EMPNO, ENAME)
VALUES (1111, '%张%'),
       (2222, '\'李\'');
COMMIT;

SELECT ENAME
FROM EMP
WHERE ENAME LIKE '%\%%';

SELECT ENAME
FROM EMP
WHERE ENAME LIKE '%$%%' ESCAPE '$';

SELECT ENAME
FROM EMP
WHERE ENAME LIKE '%\'%';

SELECT ENAME
FROM EMP
WHERE ENAME LIKE '%''%';